"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const config_1 = require("@schematics/angular/utility/config");
function getProjectName(host, project) {
    const projectName = project;
    const workspace = config_1.getWorkspace(host);
    const projects = Object.keys(workspace.projects);
    return projectName && projects.indexOf(projectName) > -1 ? projectName : workspace.defaultProject;
}
exports.getProjectName = getProjectName;
function getApplicationPath(host, projectName) {
    const rootPath = getRootPath(host, projectName);
    return rootPath ? `${rootPath}/app/` : 'src/app/';
}
exports.getApplicationPath = getApplicationPath;
function getRootPath(host, projectName) {
    const project = config_1.getWorkspace(host).projects[projectName];
    return project.sourceRoot || project.root;
}
exports.getRootPath = getRootPath;
//# sourceMappingURL=project.js.map