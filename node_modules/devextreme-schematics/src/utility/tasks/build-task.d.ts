import { TaskConfiguration, TaskConfigurationGenerator } from '@angular-devkit/schematics/src/engine';
import { NodePackageTaskOptions } from './options';
export declare class NodePackageInstallTaskOptions {
    packageManager: string;
    packageName: string;
    workingDirectory: string;
    quiet: boolean;
}
export declare class NodePackageInstallTask2 implements TaskConfigurationGenerator<NodePackageTaskOptions> {
    quiet: boolean;
    workingDirectory?: string;
    packageManager?: string;
    packageName?: string;
    constructor(workingDirectory?: string);
    constructor(options: Partial<NodePackageInstallTaskOptions>);
    toConfiguration(): TaskConfiguration<NodePackageTaskOptions>;
}
